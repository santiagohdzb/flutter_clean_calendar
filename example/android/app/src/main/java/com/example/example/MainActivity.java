package com.example.example;

import io.flutter.embedding.android.FlutterActivity;

public class MainActivity extends FlutterActivity {
  // The new embedding handles plugin registration automatically.
}
